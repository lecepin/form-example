import { defineConfig } from 'dumi';

export default defineConfig({
  themeConfig: {
    name: 'Form',
    footer: false,
  },
  logo: false,
  favicons: false,
  base: '/form-example/',
  publicPath: '/form-example/',
});
